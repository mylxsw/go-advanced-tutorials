# 本次文档修改说明

## 需求背景

原 README 已经覆盖了性能优化、问题解决和问题排查的基本思路，例如 benchmark、race test、指标、pprof、trace、压测、线上故障模式等内容。但这些内容偏原则和方向，读者读完后仍然可能不知道具体如何动手：

- 不清楚不同问题应该使用哪些工具。
- 不清楚工具的具体命令和接入方式。
- 不清楚 profile、trace、benchmark、指标里应该重点看哪些数据。
- 不清楚这些工具背后的核心原理和边界。
- 不清楚线上排障时如何把多个工具串起来形成证据链。

因此本次修改的目标是把原有“方法论式描述”补充为“可执行操作手册”，让读者能从一个线上现象出发，按照步骤采集数据、判断问题类型，并用 benchmark 或压测验证优化结果。

## 修改范围

本次只修改文档，不涉及 Go 代码、构建配置或运行时行为。

涉及文件：

- `README.md`

主要新增内容：

- 在目录中新增 `排障工具组合` 条目。
- 在第 15 章新增 `15.4 性能分析工具怎么用`。
- 在 `常见线上故障模式` 后新增 `排障工具组合` 章节。
- 扩展 `生产排障手册` 中多个故障场景的具体操作步骤。

## 详细修改内容

### 1. 新增 `15.4 性能分析工具怎么用`

该章节补充了 Go 项目常见性能分析工具的实操方法，包括：

- `go test -bench`：用于本地比较两种实现。
- `benchstat`：用于多轮 benchmark 结果对比。
- `pprof` CPU：用于定位 CPU 热点函数和调用链。
- `pprof` heap：用于分析内存分配和当前存活对象。
- `pprof` goroutine：用于排查 goroutine 泄漏和阻塞点。
- `pprof` mutex/block：用于排查锁竞争和阻塞等待。
- `go tool trace`：用于观察 Go runtime 调度、网络等待、GC、锁等待等时间线。
- `hey`、`wrk`、`vegeta`：用于压测接口并观察系统拐点。
- 指标系统：用于线上趋势观察和告警。

同时补充了每类工具的重点观察数据，例如：

- benchmark 的 `ns/op`、`B/op`、`allocs/op`。
- CPU profile 的 `flat`、`cum`、火焰图宽度。
- heap profile 的 `inuse_space`、`alloc_space`。
- goroutine profile 中重复最多的堆栈。
- mutex/block profile 中等待时间最高的位置。
- trace 中 goroutine 运行、等待调度、等待网络、等待锁、GC 影响等状态。
- 压测中的 `p50/p90/p99`、错误率、队列长度、goroutine 数、GC、下游指标。

该章节还加入了 pprof 最小接入示例，强调生产环境不要把 pprof 直接暴露到公网。

### 2. 新增 `排障工具组合`

该章节放在 `常见线上故障模式` 和 `生产排障手册` 之间，作用是把工具和故障现象建立对应关系。

新增表格覆盖以下场景：

- p99 升高。
- CPU 高。
- 内存上涨。
- goroutine 上涨。
- 锁竞争。
- 下游慢。
- 数据错误。

每个场景都给出：

- 第一手工具。
- 深入分析工具。
- 最后验证方式。

该章节还补充了一套通用排障流程：

1. 先看仪表盘确认异常指标。
2. 锁定具体接口或任务。
3. 抓 trace 拆请求路径。
4. 抓对应 profile 定位代码或阻塞点。
5. 回到代码验证假设。
6. 用 benchmark 验证局部改动。
7. 用压测验证整体效果。

核心表达是：指标负责发现异常，trace 负责拆路径，pprof 负责定位代码，benchmark 负责验证局部选择，压测负责验证系统行为。

### 3. 扩展 `生产排障手册`

原排障手册已经列出了常见问题和处理方向，本次补充了更具体的操作方法和判断依据。

#### goroutine 持续上涨

新增内容：

- 建议连续抓两次 goroutine profile，中间间隔 30 秒。
- 明确需要观察总数是否上涨、停止流量后是否回落、重复最多的堆栈位置。
- 补充对 channel send、网络 IO、timer 等常见阻塞位置的判断。

#### p99 延迟升高

新增内容：

- 给出 trace、CPU profile、heap profile 的抓取命令。
- 补充判断顺序：
  - trace 中大量等待调度通常表示并发过高或 CPU 打满。
  - network wait 高时优先看下游耗时、连接池、超时和重试。
  - CPU profile 中 JSON、排序、正则、日志函数过宽时优先减少热路径重复计算。
  - heap 的 `alloc_space` 高时继续看分配和 GC。
  - 队列长度和入队等待时间同时上涨时检查处理能力和 worker 状态。

#### CPU 高但吞吐不上去

新增内容：

- 给出 CPU profile 抓取命令。
- 补充 `pprof` 交互命令：`top`、`top -cum`、`list FunctionName`、`web`。
- 说明 `top`、`top -cum`、`list` 分别应该如何解读。

#### 数据库被打爆

新增内容：

- 明确需要观察应用侧连接池、数据库侧、接口侧、缓存侧的数据。
- 补充 `database/sql` 的 `db.Stats()` 示例代码。
- 说明 `WaitCount` 和 `WaitDuration` 持续上涨时，问题可能在数据库慢、连接池不足、慢查询过多或 fan-out 放大，而不一定是 Go 代码本身慢。

## Review 重点

建议 reviewer 重点检查以下内容：

- 新增章节是否和原教程风格一致：偏工程实践、强调取舍、避免只给口号。
- 工具命令是否准确，尤其是 `pprof`、`trace`、`benchstat`、压测命令。
- 对指标和 profile 字段的解释是否准确，例如 `flat`、`cum`、`inuse_space`、`alloc_space`、`WaitCount`、`WaitDuration`。
- 生产安全提示是否足够，例如 pprof 不应暴露公网，mutex/block profile 不宜长期高采样开启。
- 排障流程是否符合真实线上处理顺序：先观测，再定位，再验证，而不是先改代码。
- 新增内容是否有重复、过长或破坏 README 阅读节奏的问题。

## 验证情况

已执行：

```bash
git diff --check
```

结果：未发现空白或补丁格式问题。

未执行代码测试，原因是本次变更只涉及 Markdown 文档内容，不涉及 Go 代码或构建配置。

## 当前变更统计

截至生成本说明时，`README.md` 的变更统计为：

```text
README.md | 242 +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-
1 file changed, 241 insertions(+), 1 deletion(-)
```

